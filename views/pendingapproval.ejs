<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Approval</title>
    <style>
        /* App-like design for Pending Approval Page */

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #e0f7fa, #f5f5fa); /* Gradient background */
            overflow-x: hidden;
            scroll-behavior: smooth; /* Smooth scrolling behavior */
        }

        .container {
            text-align: center;
            padding: 50px 20px;
            background: linear-gradient(135deg, #c6e2ff, #e3f2fd); /* Gradient for the container */
            color: #4f6367;
            width: 100%;
            height: 80vh; /* Height of the container */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            border-radius: 20px; /* Rounded corners for the container */
        }

        h1 {
            font-size: 2.5em;
            margin: 0;
            letter-spacing: 1px;
        }

        p {
            font-size: 1.2em;
            margin: 10px 0 0;
        }

        a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #42a5f5; /* Bright blue color for contrast */
            color: white;
            text-decoration: none;
            border-radius: 12px;
            transition: background-color 0.3s ease, transform 0.3s ease;
            font-weight: bold;
            margin-top: 20px; /* Space above the button */
        }

        a:hover {
            background-color: #1e88e5; /* Darker blue on hover */
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>Pending Approval</h1>
        <p><%= message %></p>
        <a href="/">Go to Home</a>
    </div>
</body>
</html>
