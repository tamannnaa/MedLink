<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reject Doctor - Provide Reason</title>
    <style>
        /* App-like design for Doctor Rejection Reason */
        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #e0f7fa, #f5f5fa);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow-x: hidden;
        }

        h1 {
            font-size: 2.5em;
            color: #4f6367;
            margin-bottom: 20px;
        }

        form {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 80%;
            max-width: 500px;
        }

        textarea {
            width: 100%;
            height: 150px;
            padding: 10px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 1em;
            margin-bottom: 20px;
        }

        button {
            padding: 12px 25px;
            background-color: #42a5f5;
            color: white;
            text-decoration: none;
            border-radius: 12px;
            font-weight: bold;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #1e88e5;
            transform: translateY(-3px);
        }
    </style>
</head>
<body>
    <h1>Reject Doctor</h1>
    <form action="/reject/<%= doctorId %>" method="POST">
        <label for="reason">Please provide a reason for rejection:</label>
        <textarea name="reason" id="reason" required></textarea>
        <button type="submit">Reject Doctor</button>
    </form>
</body>
</html>
